import { WORDS } from '../constants/wordlist';
const queryWordList = ({ type, word, position }) => {
    console.log({
        WORDS,
        type, 
        word,
        position,
    })
}

export default queryWordList;